// this is just some pointer practice for myself, I also need to practice my styling because right now its uGly
#include <stdio.h>

int main() {
int prt = 69;
int* ptr = &prt;
printf("%d", ptr);
return 0;
}
